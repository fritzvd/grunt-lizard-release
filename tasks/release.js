var release = require('release-it');
var changelog = require('../lib/changelog');

module.exports = function (grunt) {
  grunt.registerTask('lizard-release', 'Checkout checkin, commit, changelog all the things release', function (increment) {
  });
};
